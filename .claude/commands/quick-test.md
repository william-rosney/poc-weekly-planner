# Quick Test Command

Exécute les tests essentiels du projet de manière rapide.

## Actions

1. Vérifier TypeScript: `npm run typecheck`
2. Lancer le build: `npm run build`
3. Vérifier le formatage: `npm run format:check`

Une fois les tests implémentés, cette commande les exécutera également.
